export function sampleFn(a: any, b: any) {
    return a + b + b + a;
}
export default function sample(a: string, b: string): string;
export default function sample(a: number, b: number): number;
export default function sample(a: any, b: any) {
    return a + b;
}
